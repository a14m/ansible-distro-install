#!/bin/sh
set -eu
kernel-install add-all
for entry in /boot/loader/entries/*.conf; do
  [ -e "$entry" ] || continue
  version=$(sed -n 's/^version *//p' "$entry")
  [ -d "/usr/lib/modules/$version" ] || kernel-install remove "$version"
done
